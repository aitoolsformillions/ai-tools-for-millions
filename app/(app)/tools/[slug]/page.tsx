import { notFound } from 'next/navigation'; import { tools } from '@/data/tools';
export default async function ToolDetail({params}:{params:Promise<{slug:string}>}){const {slug}=await params; const tool=tools.find(t=>t.slug===slug); if(!tool) notFound(); return <main className="container" style={{padding:'64px 0 90px'}}><div className="card" style={{padding:'clamp(24px,5vw,56px)'}}><span style={{color:'var(--blue-2)',fontWeight:800,fontSize:12}}>{tool.category.toUpperCase()}</span><h1 style={{fontSize:64,letterSpacing:'-.05em',margin:'12px 0'}}>{tool.name}</h1><p style={{fontSize:20,color:'var(--muted)',lineHeight:1.7,maxWidth:760}}>{tool.summary}</p><div style={{display:'flex',gap:12,flexWrap:'wrap',margin:'28px 0'}}><span className="glass" style={{padding:'10px 14px',borderRadius:999}}>★ {tool.rating}</span><span className="glass" style={{padding:'10px 14px',borderRadius:999}}>{tool.pricing}</span><span className="glass" style={{padding:'10px 14px',borderRadius:999}}>Best for: {tool.bestFor}</span></div><button className="btn btn-primary">Visit Official Website</button></div></main>}
