import { ToolCard } from '@/components/tool-card'; import { tools } from '@/data/tools';
export default function Tools(){return <main className="container" style={{padding:'64px 0 90px'}}><span style={{color:'var(--blue-2)',fontWeight:800,fontSize:12}}>AI TOOL EXPLORER</span><h1 style={{fontSize:56,letterSpacing:'-.04em',margin:'12px 0'}}>Find the right AI tool.</h1><p style={{color:'var(--muted)',fontSize:18}}>Search, compare, and save tools for your exact workflow.</p><input aria-label="Search AI tools" placeholder="Try: free AI for real estate marketing" style={{width:'100%',margin:'28px 0',minHeight:58,borderRadius:16,border:'1px solid var(--line)',background:'var(--surface)',color:'var(--text)',padding:'0 18px'}}/><div className="grid" style={{gridTemplateColumns:'repeat(auto-fit,minmax(260px,1fr))'}}>{tools.map(t=><ToolCard key={t.slug} tool={t}/>)}</div></main>}
