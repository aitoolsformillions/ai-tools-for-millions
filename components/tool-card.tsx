import Link from 'next/link';
import type { Tool } from '@/data/tools';
export function ToolCard({tool}:{tool:Tool}){return <article className="card" style={{padding:24,display:'flex',flexDirection:'column',gap:16}}><div style={{display:'flex',justifyContent:'space-between',gap:16}}><div><span style={{fontSize:12,color:'var(--blue-2)',textTransform:'uppercase',letterSpacing:'.12em'}}>{tool.category}</span><h3 style={{fontSize:24,margin:'8px 0'}}>{tool.name}</h3></div><span aria-label={`${tool.rating} out of 5`} style={{color:'#ffd66b'}}>★ {tool.rating}</span></div><p style={{color:'var(--muted)',lineHeight:1.65,margin:0}}>{tool.summary}</p><div style={{display:'flex',gap:10,flexWrap:'wrap',fontSize:13}}><span className="glass" style={{padding:'8px 10px',borderRadius:999}}>{tool.pricing}</span><span className="glass" style={{padding:'8px 10px',borderRadius:999}}>{tool.bestFor}</span></div><Link className="btn btn-secondary" href={`/tools/${tool.slug}`} style={{marginTop:'auto'}}>View Details</Link></article>}
